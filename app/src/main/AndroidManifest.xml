<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.lewis.ipract">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/classroom"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/classroom"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".sign_upActivity"
            android:label="@string/title_activity_sign_up"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".sign_inActivity" />
        <activity android:name=".departmentActivity" />
        <activity android:name=".csUnitsActivity" />
        <activity android:name=".listViewActivity"></activity>
        <activity android:name=".Second_year"></activity>
        <activity android:name=".Third_year"></activity>
        <activity android:name=".Fourth_year"></activity>

    </application>

</manifest>